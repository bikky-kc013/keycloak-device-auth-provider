parent=keycloak
import=common/keycloak


# The parent "keycloak" theme's own login.css must be re-listed here -
# theme.properties keys are not merged across the parent chain, so setting
# "styles" without it would drop the base theme's layout/reset styles.
styles=css/login.css css/device-auth.css
scripts=js/device-auth.js
